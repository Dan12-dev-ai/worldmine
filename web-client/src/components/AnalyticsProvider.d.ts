/** Typed surface of legacy AnalyticsProvider.jsx (Phase 10 migration seam). */
import type { FC, ReactNode } from 'react';

declare const AnalyticsProvider: FC<{ children?: ReactNode }>;
export default AnalyticsProvider;
